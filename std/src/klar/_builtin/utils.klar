// This file exists because the builtin module currently can't import other
// standard library modules. Types are re-defined here from other modules.

type SortOrder {
    .increasing // A < B
    .equal // A == B
    .decreasing // A > B
}

type BuiltinSortMode {
    .x, .y
}
