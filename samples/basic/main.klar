import klar.io
import klar.doesntExist
type #X
type #X
import invalidHere
// Supposed to be public
/* public */ type Person: Organism {
    name: String
    age: Int
    job: String
  }

yeahLetsGo()
removeThisLine(1, 2)

// Remove the () for an error with a hint + diff
func abc() = Person("John", 32, "Developer (Go)")
/* _ = */x